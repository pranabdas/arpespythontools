name: Deploy docs

on:
  schedule:
    - cron: "0 0 1 1 *"
  push:
    branches:
      - main
    paths:
      - .github/workflows/deploy-gh-pages.yml
      - docs/**
      - docusaurus.config.js
      - package.json
      - package-lock.json
      - sidebars.js
      - src/css/**
      - src/pages/**
      - static/**

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Prepare Node environment
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: "lts/*"

      - name: Build website
        run: |
          npm install
          wget https://raw.githubusercontent.com/pranabdas/espresso/main/scripts/build_docusaurus.sh
          bash build_docusaurus.sh

      - name: Deploy on gh-pages
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d475ec49e8ef4d45e # v4.0.0
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"

      - name: Build for cloudflare-pages
        run: |
          sed -i 's|^const site_url\s*=.*|const site_url="https://pranab.pages.dev";|I' docusaurus.config.js
          bash build_docusaurus.sh

      - name: Deploy on cloudflare
        if: ${{ github.event_name != 'schedule' }}
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d475ec49e8ef4d45e # v4.0.0
        with:
          personal_token: ${{ secrets.CLOUDFLARE_DEPLOY_KEY }}
          publish_dir: build
          external_repository: ${{ github.repository_owner }}/${{ secrets.CLOUDFLARE_REPO_NAME }}
          publish_branch: cf-pages
          destination_dir: ${{ github.event.repository.name }}
          enable_jekyll: true
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
